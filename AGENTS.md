# 项目约定

## 沟通方式

- 使用中文交流。
- 遇到歧义或疑问时，**直接向用户确认和问询**，列出具体问题（一次性列全），不要循环推测、不要反复推倒重来。
- 用户的答案明确后再动手实现，避免不必要的试探性操作消耗 token。

## 输出纪律（严格）

- 完成任务后直接总结改动并收尾，**禁止追加**试探性提议（如"要不要用浏览器看看""要不要截图""要不要我再做 XX"）。
- 不做任何浏览器、截图、视觉验证类操作，除非用户明确要求；用户没要求就是不需要。
- 动手前先想清楚改动是否必要；纯装饰、纯审美类的调整不主动做，先问用户。
